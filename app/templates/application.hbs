  {{#paper-toolbar}}
    {{#paper-toolbar-tools}}
      {{#paper-button iconButton=true}}
        {{paper-icon "menu"}}
      {{/paper-button}}
      {{#link-to 'goals'}}
        <h2>Pomodorify</h2>
      {{/link-to}}
      <span class="flex"></span>
      {{#if userLoggedIn}}
        {{#paper-button onClick=(route-action 'logout') id='sign-out' raised=true}}
          Sign Out
        {{/paper-button}}
      {{/if}}
    {{/paper-toolbar-tools}}
  {{/paper-toolbar}}
<div class='main-content'>
  {{outlet}}
</div>
